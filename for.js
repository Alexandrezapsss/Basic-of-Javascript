//Struct FOR
//make the automatic multiple the user go insert the number for multiple
let num= Number(prompt('Digite a tabuada que precisa: '));
for(i = 0; i <= 10; i++){
    console.log(`${num} * ${i}= ${num * i}`);
}